Create: Mail Rail.

This is a tiny addon that adds a singular task for the create train routes, that being a "Pick up a package but only if nothing else is headed there to grab a package". the method in which I do so is probably dog ass, but it seems to work so IDC that much 
